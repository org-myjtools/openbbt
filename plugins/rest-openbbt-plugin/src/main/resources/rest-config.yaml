steps:
  - 'rest.step1':
      expressions:
        en: 'What is the current stock price of {{ ticker }}?'
        es: '¿Cuál es el precio actual de la acción de {{ ticker }}?'
      parameters:
        - name: 'ticker'
          type: 'string'